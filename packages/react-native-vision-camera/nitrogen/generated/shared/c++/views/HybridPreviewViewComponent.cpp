///
/// HybridPreviewViewComponent.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © 2025 Marc Rousavy @ Margelo
///

#include "HybridPreviewViewComponent.hpp"

#include <string>
#include <exception>
#include <utility>
#include <NitroModules/NitroDefines.hpp>
#include <NitroModules/JSIConverter.hpp>
#include <react/renderer/core/RawValue.h>
#include <react/renderer/core/ShadowNode.h>
#include <react/renderer/core/ComponentDescriptor.h>
#include <react/renderer/components/view/ViewProps.h>

namespace margelo::nitro::camera::views {

  extern const char HybridPreviewViewComponentName[] = "PreviewView";

  HybridPreviewViewProps::HybridPreviewViewProps(const react::PropsParserContext& context,
                                                 const HybridPreviewViewProps& sourceProps,
                                                 const react::RawProps& rawProps):
    react::ViewProps(context, sourceProps, rawProps, filterObjectKeys),
    session([&]() -> CachedProp<std::optional<std::shared_ptr<HybridCameraSessionSpec>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("session", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.session;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::shared_ptr<HybridCameraSessionSpec>>>::fromRawValue(*runtime, value, sourceProps.session);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PreviewView.session: ") + exc.what());
      }
    }()),
    hybridRef([&]() -> CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPreviewViewSpec>& /* ref */)>>> {
      try {
        const react::RawValue* rawValue = rawProps.at("hybridRef", nullptr, nullptr);
        if (rawValue == nullptr) return sourceProps.hybridRef;
        const auto& [runtime, value] = (std::pair<jsi::Runtime*, jsi::Value>)*rawValue;
        return CachedProp<std::optional<std::function<void(const std::shared_ptr<HybridPreviewViewSpec>& /* ref */)>>>::fromRawValue(*runtime, value.asObject(*runtime).getProperty(*runtime, "f"), sourceProps.hybridRef);
      } catch (const std::exception& exc) {
        throw std::runtime_error(std::string("PreviewView.hybridRef: ") + exc.what());
      }
    }()) { }

  HybridPreviewViewProps::HybridPreviewViewProps(const HybridPreviewViewProps& other):
    react::ViewProps(),
    session(other.session),
    hybridRef(other.hybridRef) { }

  bool HybridPreviewViewProps::filterObjectKeys(const std::string& propName) {
    switch (hashString(propName)) {
      case hashString("session"): return true;
      case hashString("hybridRef"): return true;
      default: return false;
    }
  }

  HybridPreviewViewComponentDescriptor::HybridPreviewViewComponentDescriptor(const react::ComponentDescriptorParameters& parameters)
    : ConcreteComponentDescriptor(parameters,
                                  react::RawPropsParser(/* enableJsiParser */ true)) {}

  std::shared_ptr<const react::Props> HybridPreviewViewComponentDescriptor::cloneProps(const react::PropsParserContext& context,
                                                                                       const std::shared_ptr<const react::Props>& props,
                                                                                       react::RawProps rawProps) const {
    // 1. Prepare raw props parser
    rawProps.parse(rawPropsParser_);
    // 2. Copy props with Nitro's cached copy constructor
    return HybridPreviewViewShadowNode::Props(context, /* & */ rawProps, props);
  }

#ifdef ANDROID
  void HybridPreviewViewComponentDescriptor::adopt(react::ShadowNode& shadowNode) const {
    // This is called immediately after `ShadowNode` is created, cloned or in progress.
    // On Android, we need to wrap props in our state, which gets routed through Java and later unwrapped in JNI/C++.
    auto& concreteShadowNode = dynamic_cast<HybridPreviewViewShadowNode&>(shadowNode);
    const HybridPreviewViewProps& props = concreteShadowNode.getConcreteProps();
    HybridPreviewViewState state;
    state.setProps(props);
    concreteShadowNode.setStateData(std::move(state));
  }
#endif

} // namespace margelo::nitro::camera::views

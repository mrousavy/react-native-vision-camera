///
/// HybridRecorderSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `RecordingFinishedReason` to properly resolve imports.
namespace margelo::nitro::camera { enum class RecordingFinishedReason; }

#include <string>
#include <NitroModules/Promise.hpp>
#include "RecordingFinishedReason.hpp"
#include <functional>
#include <exception>
#include <optional>

namespace margelo::nitro::camera {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Recorder`
   * Inherit this class to create instances of `HybridRecorderSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridRecorder: public HybridRecorderSpec {
   * public:
   *   HybridRecorder(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridRecorderSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridRecorderSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridRecorderSpec() override = default;

    public:
      // Properties
      virtual bool getIsRecording() = 0;
      virtual bool getIsPaused() = 0;
      virtual double getRecordedDuration() = 0;
      virtual double getRecordedFileSize() = 0;
      virtual std::string getFilePath() = 0;

    public:
      // Methods
      virtual std::shared_ptr<Promise<void>> startRecording(const std::function<void(const std::string& /* filePath */, RecordingFinishedReason /* reason */)>& onRecordingFinished, const std::function<void(const std::exception_ptr& /* error */)>& onRecordingError, const std::optional<std::function<void()>>& onRecordingPaused, const std::optional<std::function<void()>>& onRecordingResumed) = 0;
      virtual std::shared_ptr<Promise<void>> stopRecording() = 0;
      virtual std::shared_ptr<Promise<void>> pauseRecording() = 0;
      virtual std::shared_ptr<Promise<void>> resumeRecording() = 0;
      virtual std::shared_ptr<Promise<void>> cancelRecording() = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Recorder";
  };

} // namespace margelo::nitro::camera

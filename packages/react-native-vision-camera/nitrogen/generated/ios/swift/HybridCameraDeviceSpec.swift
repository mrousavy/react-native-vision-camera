///
/// HybridCameraDeviceSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import NitroModules

/// See ``HybridCameraDeviceSpec``
public protocol HybridCameraDeviceSpec_protocol: HybridObject {
  // Properties
  var id: String { get }
  var modelID: String { get }
  var localizedName: String { get }
  var manufacturer: String { get }
  var type: DeviceType { get }
  var position: CameraPosition { get }
  var physicalDevices: [(any HybridCameraDeviceSpec)] { get }
  var isVirtualDevice: Bool { get }
  var supportedPixelFormats: [PixelFormat] { get }
  var supportsPhotoHDR: Bool { get }
  var supportedVideoDynamicRanges: [DynamicRange] { get }
  var supportedFPSRanges: [Range] { get }
  var supportsPreviewImage: Bool { get }
  var supportsSpeedQualityPrioritization: Bool { get }
  var focalLength: Double? { get }
  var lensAperture: Double { get }
  var isContinuityCamera: Bool { get }
  var companionDeskViewCamera: (any HybridCameraDeviceSpec)? { get }
  var mediaTypes: [MediaType] { get }
  var supportsFocusMetering: Bool { get }
  var supportsFocusLocking: Bool { get }
  var supportsSmoothAutoFocus: Bool { get }
  var supportsExposureMetering: Bool { get }
  var supportsExposureLocking: Bool { get }
  var supportsExposureBias: Bool { get }
  var minExposureBias: Double { get }
  var maxExposureBias: Double { get }
  var supportsWhiteBalanceMetering: Bool { get }
  var maxWhiteBalanceGain: Double { get }
  var supportsWhiteBalanceLocking: Bool { get }
  var hasFlash: Bool { get }
  var hasTorch: Bool { get }
  var supportsLowLightBoost: Bool { get }
  var minZoom: Double { get }
  var maxZoom: Double { get }
  var zoomLensSwitchFactors: [Double] { get }
  var supportsDistortionCorrection: Bool { get }

  // Methods
  func getSupportedResolutions(outputStreamType: OutputStreamType) throws -> [Size]
  func supportsOutput(output: (any HybridCameraOutputSpec)) throws -> Bool
  func supportsFPS(fps: Double) throws -> Bool
  func supportsVideoStabilizationMode(videoStabilizationMode: TargetStabilizationMode) throws -> Bool
  func supportsPreviewStabilizationMode(previewStabilizationMode: TargetStabilizationMode) throws -> Bool
  func isSessionConfigSupported(config: (any HybridCameraSessionConfigSpec)) throws -> Bool
}

public extension HybridCameraDeviceSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject CameraDevice]"
  }
}

/// See ``HybridCameraDeviceSpec``
open class HybridCameraDeviceSpec_base {
  private weak var cxxWrapper: HybridCameraDeviceSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridCameraDeviceSpec_cxx {
  #if DEBUG
    guard self is any HybridCameraDeviceSpec else {
      fatalError("`self` is not a `HybridCameraDeviceSpec`! Did you accidentally inherit from `HybridCameraDeviceSpec_base` instead of `HybridCameraDeviceSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridCameraDeviceSpec_cxx(self as! any HybridCameraDeviceSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the CameraDevice HybridObject.
 * Implement this protocol to create Swift-based instances of CameraDevice.
 * ```swift
 * class HybridCameraDevice : HybridCameraDeviceSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridCameraDeviceSpec = HybridCameraDeviceSpec_protocol & HybridCameraDeviceSpec_base

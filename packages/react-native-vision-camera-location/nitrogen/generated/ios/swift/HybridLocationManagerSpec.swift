///
/// HybridLocationManagerSpec.swift
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

import VisionCamera
import NitroModules

/// See ``HybridLocationManagerSpec``
public protocol HybridLocationManagerSpec_protocol: HybridObject {
  // Properties
  var lastKnownLocation: (any HybridLocationSpec)? { get }
  var locationPermissionStatus: PermissionStatus { get }

  // Methods
  func requestLocationPermission() throws -> Promise<Bool>
  func startUpdating() throws -> Promise<Void>
  func stopUpdating() throws -> Promise<Void>
  func addOnLocationChangedListener(callback: @escaping (_ location: (any HybridLocationSpec)) -> Void) throws -> ListenerSubscription
}

public extension HybridLocationManagerSpec_protocol {
  /// Default implementation of ``HybridObject.toString``
  func toString() -> String {
    return "[HybridObject LocationManager]"
  }
}

/// See ``HybridLocationManagerSpec``
open class HybridLocationManagerSpec_base {
  private weak var cxxWrapper: HybridLocationManagerSpec_cxx? = nil
  public init() { }
  public func getCxxWrapper() -> HybridLocationManagerSpec_cxx {
  #if DEBUG
    guard self is any HybridLocationManagerSpec else {
      fatalError("`self` is not a `HybridLocationManagerSpec`! Did you accidentally inherit from `HybridLocationManagerSpec_base` instead of `HybridLocationManagerSpec`?")
    }
  #endif
    if let cxxWrapper = self.cxxWrapper {
      return cxxWrapper
    } else {
      let cxxWrapper = HybridLocationManagerSpec_cxx(self as! any HybridLocationManagerSpec)
      self.cxxWrapper = cxxWrapper
      return cxxWrapper
    }
  }
}

/**
 * A Swift base-protocol representing the LocationManager HybridObject.
 * Implement this protocol to create Swift-based instances of LocationManager.
 * ```swift
 * class HybridLocationManager : HybridLocationManagerSpec {
 *   // ...
 * }
 * ```
 */
public typealias HybridLocationManagerSpec = HybridLocationManagerSpec_protocol & HybridLocationManagerSpec_base
